---

# 🚀 Урок: Основы HTML + CSS + JavaScript

## 📌 Цель урока

На этом занятии мы:

* Разберём структуру `index.html`
* Изучим важные HTML-теги
* Подключим CSS
* Напишем базовые стили
* Подключим JavaScript
* Научимся менять страницу через JS

---

# 1️⃣ Структура проекта

Создаём папку проекта:

```
project/
│
├── index.html
├── styles.css
└── script.js
```

---

# 2️⃣ Базовая структура `index.html`

Создаём файл `index.html`:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мой первый сайт</title>

    <!-- Подключаем CSS -->
    <link rel="stylesheet" href="styles.css">
</head>
<body>

    <header>
        <h1>Добро пожаловать!</h1>
        <p class="subtitle">Это мой первый HTML сайт</p>
    </header>

    <main>
        <section>
            <h2>Обо мне</h2>
            <p id="aboutText">Я изучаю веб-разработку.</p>
        </section>

        <section>
            <button id="changeBtn">Изменить текст</button>
            <button id="colorBtn">Сменить цвет</button>
        </section>
    </main>

    <footer>
        <p>© 2026 Мой сайт</p>
    </footer>

    <!-- Подключаем JS в конце -->
    <script src="script.js"></script>
</body>
</html>
```

---

# 3️⃣ Важные HTML теги

| Тег               | Назначение               |
| ----------------- | ------------------------ |
| `<!DOCTYPE html>` | Объявляет HTML5          |
| `<html>`          | Корневой элемент         |
| `<head>`          | Метаинформация           |
| `<body>`          | Видимая часть страницы   |
| `<h1> - <h6>`     | Заголовки                |
| `<p>`             | Параграф                 |
| `<div>`           | Блок                     |
| `<section>`       | Логическая секция        |
| `<header>`        | Шапка                    |
| `<footer>`        | Подвал                   |
| `<button>`        | Кнопка                   |
| `class`           | Группировка элементов    |
| `id`              | Уникальный идентификатор |

---

# 4️⃣ Базовые стили (styles.css)

Создаём `styles.css`:

```css
/* Сброс отступов */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Базовый стиль страницы */
body {
    font-family: Arial, sans-serif;
    background-color: #f4f6f9;
    padding: 40px;
}

/* Заголовок */
header {
    margin-bottom: 30px;
}

h1 {
    color: #222;
}

.subtitle {
    color: #777;
    margin-top: 8px;
}

/* Кнопки */
button {
    padding: 10px 20px;
    margin-top: 15px;
    margin-right: 10px;
    border: none;
    background-color: #007BFF;
    color: white;
    border-radius: 6px;
    cursor: pointer;
    transition: 0.3s;
}

button:hover {
    background-color: #0056b3;
}
```

---

# 5️⃣ Подключение JavaScript

Создаём `script.js`:

```javascript
// Получаем элементы
const changeBtn = document.getElementById("changeBtn");
const colorBtn = document.getElementById("colorBtn");
const aboutText = document.getElementById("aboutText");

// Событие изменения текста
changeBtn.addEventListener("click", function () {
    aboutText.textContent = "Теперь текст изменён через JavaScript!";
});

// Событие смены цвета
colorBtn.addEventListener("click", function () {
    document.body.style.backgroundColor = "#ffeeba";
});
```

---

# 6️⃣ Что здесь происходит?

### 🔹 document.getElementById()

Находит элемент по id.

### 🔹 addEventListener()

Добавляет обработчик события (например, клик).

### 🔹 textContent

Меняет текст внутри элемента.

### 🔹 style

Позволяет менять CSS через JS.

---

# 7️⃣ Как это работает вместе?

1. HTML создаёт структуру
2. CSS делает страницу красивой
3. JavaScript добавляет интерактивность

Это три основы веб-разработки.

---

# 8️⃣ Практическое задание

## 🔹 Задание 1

Добавьте кнопку, которая будет:

* увеличивать размер текста
* менять цвет заголовка

## 🔹 Задание 2

Добавьте input и кнопку, чтобы:

* пользователь вводил своё имя
* сайт выводил: "Привет, Имя!"

---

# 9️⃣ Дополнительное усложнение (для продвинутых)

Попробуйте:

* Использовать `classList.add()` / `classList.toggle()`
* Создать тёмную тему
* Добавить анимацию через CSS

---

# 🔥 Мини-итог

Вы научились:

* Создавать HTML страницу
* Подключать CSS
* Подключать JS
* Управлять DOM
* Делать интерактивный сайт

---
