````md
# Практическое занятие  
## Создание веб-приложения на React (JS) и Flask (Python) с общими требованиями

Преподаватель: я  
Формат: практическая работа  
Длительность: 3–4 часа

---

## 🎯 Цель занятия

На этом занятии вы разрабатываете одно и то же веб-приложение,  
но на разных платформах:

- либо на **React (JavaScript)** — клиентское SPA,
- либо на **Flask (Python)** — backend API,
- либо в связке **React + Flask**.

Функциональные требования у всех одинаковые.

---

## 🧩 Общая идея приложения

Мини-приложение **TaskHub** — список задач.

---

## 📦 Модель данных

```json
{
  "id": "string",
  "title": "string",
  "status": "todo | doing | done",
  "createdAt": "ISO string"
}
````

---

## ✅ Общие требования (обязательные для всех)

Приложение должно уметь:

1. показывать список задач
2. создавать задачу
3. менять статус задачи
4. удалять задачу
5. искать задачи по названию
6. фильтровать по статусу
7. валидировать ввод (пустой title запрещён)
8. показывать состояния:

   * loading
   * empty
   * error

---

## 🧑‍💻 Выбор трека

В начале занятия вы выбираете один вариант:

* Трек A — React (JS)
* Трек B — Flask (Python)
* Трек C — React + Flask (full-stack, бонус)

---

# 🟦 Трек A — React (JavaScript)

Вы разрабатываете SPA.

---

## 📁 Рекомендуемая структура компонентов

* TaskApp
* TaskForm
* TaskFilters
* TaskList
* TaskItem

---

## ⚙️ Состояние приложения

Минимально должны быть состояния:

* tasks
* query
* statusFilter
* loading
* error

Фильтрацию и поиск необходимо выполнять на клиенте.

---

## 💾 Источник данных (на выбор)

### Вариант 1 — без сервера

Хранение задач в:

```
localStorage
```

При старте:

* загрузить задачи из localStorage

При изменениях:

* сохранять в localStorage

---

### Вариант 2 — через API (если используется Flask)

Использовать HTTP-запросы:

* GET /api/tasks
* POST /api/tasks
* PATCH /api/tasks/:id
* DELETE /api/tasks/:id

---

## 📌 Обязательные требования для React

* без перезагрузки страницы
* есть loading при загрузке
* есть сообщение об ошибке
* форма не позволяет создать пустую задачу

---

## ⭐ Бонус для React

* debounce для поиска (300–500 мс)
* оптимистичное обновление статуса

---

# 🟩 Трек B — Flask (Python)

Вы разрабатываете backend API.

---

## 🌐 Базовый путь

```
/api
```

---

## 🔌 Endpoints

### Получение всех задач

```
GET /api/tasks
```

Ответ:

```json
{
  "data": []
}
```

---

### Создание задачи

```
POST /api/tasks
```

Body:

```json
{
  "title": "Buy milk"
}
```

---

### Обновление задачи

```
PATCH /api/tasks/<id>
```

Body:

```json
{
  "title": "New title",
  "status": "doing"
}
```

(разрешено передавать только одно из полей)

---

### Удаление задачи

```
DELETE /api/tasks/<id>
```

---

## ❗ Валидация и ошибки

Обязательно:

### Пустой title

HTTP 400

```json
{ "error": "TITLE_REQUIRED" }
```

---

### Несуществующий id

HTTP 404

```json
{ "error": "NOT_FOUND" }
```

---

## 🗄 Хранилище

Минимальный вариант:

* in-memory список или словарь

Бонус:

* SQLite + SQLAlchemy

---

## 🌍 CORS

Если фронт и backend запускаются отдельно —
необходимо включить CORS.

---

## 📌 Обязательные требования для Flask

* корректные HTTP-коды
* единый JSON-формат
* реализованы все 4 endpoint
* есть валидация входных данных

---

# 🟪 Трек C — React + Flask (бонус)

React работает как клиент, Flask — как API.

---

## Интеграционный сценарий

React должен:

1. загрузить задачи через GET
2. создать задачу через POST
3. изменить статус через PATCH
4. удалить задачу через DELETE

---

# 🧪 Общие тестовые сценарии (для всех)

Вы должны показать:

1. попытку создать задачу с пустым названием
2. создание минимум трёх задач
3. работу фильтра по статусу
4. работу поиска
5. удаление задачи
6. отображение error-состояния (например, сервер недоступен)

---

# ⭐ Дополнительные задания (по желанию)

1. сортировка по createdAt (новые сверху)
2. пагинация на сервере
3. mock-авторизация через заголовок

```
X-User: demo
```

---

# 📦 Что сдаётся

1. репозиторий или архив с проектом
2. README с инструкцией запуска
3. скриншоты или короткое видео:

   * список задач
   * добавление
   * фильтр
   * поиск

---

# 🧠 Критерии оценки

Оценивается:

* корректная реализация требований
* читаемость кода
* обработка ошибок
* логика состояния интерфейса

---

# ⏱ Рекомендуемый тайминг

1. Выбор трека и подготовка проекта — 15 мин
2. CRUD и базовая логика — 90 мин
3. Поиск, фильтры, валидация — 40 мин
4. Ошибки, loading, доработка — 20–30 мин

---

Ваша задача — не просто сделать интерфейс или API,
а показать умение проектировать и реализовывать веб-приложение
с нормальной архитектурой и обработкой ошибок.

```
```
